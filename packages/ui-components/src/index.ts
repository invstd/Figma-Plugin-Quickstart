/**
 * @inversestudio/figma-ui-components
 * Pre-validated components that enforce Figma design system rules.
 * Use these instead of raw style objects to avoid violations.
 */

export { NativeText } from './NativeText';
export { SemanticBadge } from './SemanticBadge';
export type { NativeTextProps, SemanticBadgeProps } from './types';
